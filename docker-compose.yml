services:
  bot:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - .env:/app/.env
    environment:
      - DATABASE_URL=sqlite:///app/data/fisherman.db
      - RUST_LOG=info
    restart: always
